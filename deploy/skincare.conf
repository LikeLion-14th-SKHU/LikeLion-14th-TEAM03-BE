upstream skincare {
    server 127.0.0.1:8081;
}

server {
    listen 80;
    server_name skincare.duckdns.org;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name skincare.duckdns.org;

    ssl_certificate /etc/letsencrypt/live/skincare.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/skincare.duckdns.org/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://skincare;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
